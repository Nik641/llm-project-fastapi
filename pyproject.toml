[project]
name = "llm-project-fastapi"
version = "0.1.0"
description = "My LLM GPT project with fastapi"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "fastapi>=0.141.1",
    "g4f>=8.3.0",
    "sqlalchemy>=2.0.52",
    "uvicorn>=0.52.4",
]
